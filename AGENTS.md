# Local Agent Instructions (private)
#
# Scope: ./panel-notes/*
#
# Panel Notes - CSV-backed breaker map
#

- App data lives at `./panel-notes/data` (bind mounted).
- Runs on port 8080. Compose target: `docker compose up -d panel_notes`.
