version: '3.9'
services:
  panel-notes:
    build: .
    restart: unless-stopped
    ports:
      - '8080:8080'
    environment:
      - PORT=8080
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
